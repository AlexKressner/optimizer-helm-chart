apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: {{ .Release.Namespace }}
  labels:
    app: backend-service
spec:
  ports:
  - port: 6379
  selector:
    app: backend
  type: ClusterIP
